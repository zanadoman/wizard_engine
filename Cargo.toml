[package]
name = "srv"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { version = "*", default-features = true }
tokio = { version = "*", features = [
    "io-util",
    "macros",
    "net",
    "rt-multi-thread",
    "sync",
    "time",
] }
zerocopy = { version = "*", features = ["derive"] }

[[bin]]
name = "udp"
path = "./srv/udp.rs"

[[bin]]
name = "tcp"
path = "./srv/tcp.rs"
